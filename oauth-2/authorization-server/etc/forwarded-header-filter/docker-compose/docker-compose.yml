# docker-compose -p forwarded-header-filter up -d
# nginx(host 9000 포트)가 리버스 프록시로 authorization server(host 8091)에 전달한다.

services:
  nginx:
    image: nginx:1.27
    ports:
      - "9000:80"
    volumes:
      - ./nginx.conf:/etc/nginx/conf.d/default.conf:ro
