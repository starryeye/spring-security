# docker-compose -p oauth2-authorization-purge up -d

services:
  mysql:
    image: mysql:8
    environment:
      MYSQL_ROOT_PASSWORD: 1111
      MYSQL_DATABASE: oauth2_authorization_purge
    ports:
      - "3306:3306"
