# docker-compose -p custom-registered-client-repository up -d

services:
  mysql:
    image: mysql:8
    environment:
      MYSQL_ROOT_PASSWORD: 1111
      MYSQL_DATABASE: custom_registered_client_repository
    ports:
      - "3306:3306"
