# docker-compose -p custom-oauth2-authorization-service up -d

services:
  mysql:
    image: mysql:8
    environment:
      MYSQL_ROOT_PASSWORD: 1111
      MYSQL_DATABASE: custom_oauth2_authorization_service
    ports:
      - "3306:3306"
